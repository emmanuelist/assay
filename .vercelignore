# Nothing here is needed to build or serve the app.
# .git alone was 62MB of the 122MB archive — binary churn from re-committing
# the demo video — and the build never reads it.
.git/
film/
recon/
.playwright-mcp/
*.png
*.mp4
*.mp3
*.wav

# the demo video ships with the site
!public/assay-demo.mp4
!public/assay-poster.jpg
docker-compose.yml
